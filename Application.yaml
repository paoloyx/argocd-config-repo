apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: golive-player-web
  namespace: argocd
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: default
  source:
    repoURL: https://github.com/paoloyx/argocd-code-repo
    path: helm
    helm:
      values: |
        image:
          tag: 1.2.1
        service:
          port: 9000
      version: v3
  destination:
    server: https://kubernetes.default.svc
    namespace: default
  syncPolicy:
    automated:
      selfHeal: true
      prune: true
    syncOptions:
      - CreateNamespace=false